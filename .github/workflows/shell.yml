name: Shell Commands

on:
  push:
    branches:
      - main

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
      
    steps:
      - name: Echo String 
        run: echo "DevOps"

      - name: Multiline Script
        run: |
          node -v
          npm -v
